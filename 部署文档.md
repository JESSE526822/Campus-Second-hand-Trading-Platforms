# 校园二手物品拍卖管理系统 - 部署文档

## 项目概述

这是一个基于Spring Boot和Vue 3的校园二手物品拍卖管理系统，支持用户注册登录、商品发布、收藏、购物车、交易等功能。

## 系统要求

### 后端要求
- JDK 17 或更高版本
- Maven 3.6 或更高版本
- MySQL 8.0 或更高版本

### 前端要求
- Node.js 16 或更高版本
- npm 8 或更高版本

## 部署步骤

### 1. 数据库配置

#### 1.1 创建数据库
```sql
CREATE DATABASE campus_auction CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

#### 1.2 配置数据库连接
编辑文件：`src/main/resources/application.properties`

修改以下配置：
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/campus_auction?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
spring.datasource.username=你的数据库用户名
spring.datasource.password=你的数据库密码
```

### 2. 准备商品数据文件

确保项目根目录下有`商品数据.csv`文件，该文件用于初始化商品数据。

CSV文件格式：
```
用户名,分类,标题,价格,图片URL,配送方式,售后,成色
nick,电子产品,苹果iPhone 13,5999,https://example.com/iphone13.jpg,快递发货,7天无理由,全新
...
```

### 3. 后端部署

#### 3.1 进入后端目录
```bash
cd d:\HuaweiMoveData\Users\15806\Desktop\基于spring boot的校园二手物品拍卖管理系统
```

#### 3.2 安装依赖
```bash
mvn clean install
```

#### 3.3 启动后端服务
```bash
mvn spring-boot:run
```

后端服务将在 `http://localhost:8080` 启动。

**重要说明：**
- 首次启动时，系统会自动创建所有数据库表
- 系统会自动初始化用户数据（包括4个普通用户和1个管理员）
- 系统会自动从`商品数据.csv`文件导入商品数据

默认用户账号：
- 普通用户：nick, peter, bill, robin（密码都是：123456）
- 管理员：admin（密码：123456）

### 4. 前端部署

#### 4.1 进入前端目录
```bash
cd frontend
```

#### 4.2 安装依赖
```bash
npm install
```

#### 4.3 启动前端服务
```bash
npm run dev
```

前端服务将在 `http://localhost:5173` 启动。

### 5. 访问系统

打开浏览器，访问：`http://localhost:5173`

## 生产环境部署建议

### 后端打包
```bash
mvn clean package
```

打包后的JAR文件位于：`target/campus-auction-0.0.1-SNAPSHOT.jar`

运行JAR文件：
```bash
java -jar target/campus-auction-0.0.1-SNAPSHOT.jar
```

### 前端打包
```bash
npm run build
```

打包后的静态文件位于：`frontend/dist`

可以使用Nginx等Web服务器托管这些静态文件。

## 数据库表结构

系统会自动创建以下表：

1. **users** - 用户表
   - id: 用户ID
   - username: 用户名
   - password: 密码
   - email: 邮箱
   - phone: 电话
   - role: 角色（user/admin）
   - created_at: 创建时间
   - last_login_at: 最后登录时间
   - status: 状态

2. **products** - 商品表
   - id: 商品ID
   - title: 商品标题
   - name: 商品名称
   - description: 商品描述
   - price: 价格
   - category: 分类
   - status: 状态（for_sale/sold）
   - shipping_method: 配送方式
   - after_sales: 售后政策
   - product_condition: 商品成色
   - image_url: 图片URL
   - user_id: 用户ID
   - owner_id: 所有者ID
   - created_at: 创建时间
   - updated_at: 更新时间

3. **favorites** - 收藏表
   - id: 收藏ID
   - user_id: 用户ID
   - product_id: 商品ID
   - created_at: 创建时间

4. **transactions** - 交易表
   - id: 交易ID
   - buyer_id: 买家ID
   - seller_id: 卖家ID
   - product_id: 商品ID
   - price: 价格
   - quantity: 数量
   - status: 状态
   - created_at: 创建时间

5. **cart** - 购物车表
   - id: 购物车ID
   - user_id: 用户ID
   - product_id: 商品ID
   - quantity: 数量
   - created_at: 创建时间

6. **messages** - 消息表
   - id: 消息ID
   - sender_id: 发送者ID
   - receiver_id: 接收者ID
   - content: 消息内容
   - created_at: 创建时间

7. **logs** - 日志表
   - id: 日志ID
   - action: 操作
   - user: 用户
   - ip: IP地址
   - details: 详情
   - created_at: 创建时间

## 常见问题

### 1. 数据库连接失败
检查MySQL服务是否启动，数据库用户名密码是否正确。

### 2. 商品数据导入失败
确保`商品数据.csv`文件存在于项目根目录，且格式正确。

### 3. 前端无法连接后端
检查后端服务是否正常启动，端口是否被占用。

### 4. 端口冲突
如果8080端口被占用，可以在`application.properties`中修改：
```properties
server.port=8081
```

如果5173端口被占用，可以在`frontend/vite.config.js`中修改。

## 技术栈

### 后端
- Spring Boot 3.x
- Spring MVC
- Spring JDBC
- MySQL Connector/J

### 前端
- Vue 3
- Vite
- Axios
- Element Plus

## 联系方式

如有问题，请联系项目维护人员。
